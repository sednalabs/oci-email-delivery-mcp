# OCI Email Delivery MCP Capability Matrix

| workflow | tool | class | inputs | data source | proof | redaction | negative tests | docs | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| First-run readiness | `oci_email_status` | read | optional compartment override | OCI CLI profile, Email Delivery sender/domain/suppression list commands | profile can call read-only OCI APIs; sender/domain counts; suppression query outcome | profile name only, OCID hash, no raw sender local parts in public docs | missing compartment, missing profile, empty suppression stdout | README, live-proof matrix | implemented |
| Pilot aggregate monitoring | `oci_email_metrics` | read | `start_time`, `end_time`, optional `interval` using OCI shorthand or supported ISO-8601 forms, optional sender/domain filters | OCI Monitoring `oci_emaildelivery` namespace | fixed metric definitions discovered and summarized for the window; supported interval inputs normalize case-insensitively to canonical OCI shorthand before query construction | no raw OCIDs; query filters summarized | unavailable metric, empty stdout, invalid interval, ISO interval normalization, capped missing evidence | README, monitoring runbook, live-proof matrix | implemented |
| Local send-ledger window | `oci_email_ledger_window` | sensitive-read summary | `start_time`, `end_time`, optional sender domain, campaign id, batch id, message id, correlation id, limit; configured `OCI_MCP_LEDGER_PATH` | private local JSONL send ledger | bounded redacted row summaries plus counts needed for later event correlation; every present timestamp alias must be strict UTC and agree on one instant before filtering; provider, sender, and raw/prehashed campaign or batch aliases must reconcile before selector narrowing, with relevant contradictory rows retained as invalid evidence; exact OCI trace proof separately requires the selected row's provider hash to match the bounded OCI Email Delivery identity allowlist; a 20-hex campaign/batch filter preserves both legitimate raw-id and prehash interpretations; case-preserving opaque message/correlation filters are applied before the returned-row cap; prehashed trace fields must be valid 20-hex values from the same contract, coexisting raw/prehashed values must agree, and contradictory rows claiming a requested trace filter remain blocking invalid-row evidence | provider/recipient/campaign/batch/template/subject values use their field-appropriate redacted hashes; opaque message/correlation values use case-preserving hashes; recipient and sender domains only | missing ledger path, invalid JSON rows, malformed/null/whitespace-padded/conflicting timestamp alias, malformed or contradictory provider/sender/campaign/batch selector alias, filtered selector contradiction retained as invalid evidence, hash-shaped raw/prehash selector ambiguity, capped rows, missing, malformed, or contradictory message/correlation key, filtered trace contradiction retained as invalid evidence, missing recipient or provider key for exact proof, non-OCI provider authority for exact proof, case-distinct or other trace-filtered no-match | README, monitoring runbook, live-proof matrix | implemented |
| Event ingestion visibility | `oci_email_events` | read | UTC window, optional whitelisted action/message/header/receiving-domain filters, optional post-summary source-domain filter, limit | OCI Logging Search Email Delivery logs | bounded event summaries from structurally recognized OutboundAccepted/OutboundRelayed records with timestamps inside the requested half-open UTC window and request-filter readback; string UTC aliases and OCI numeric outer `datetime` epoch milliseconds must agree on one instant; every present recipient/message-id alias must be a non-empty string and all aliases for one identity must agree; unknown non-empty actions are retained only as the safe `unknown` bucket and make evidence partial; source-domain matching uses the authoritative record `source` Email Domain after redacted summary parsing and never derives lane authority from sender/envelope addresses; aggregate pre/post-filter counts and distinct/duplicate redacted recipient-message counts avoid overreading repeated event records as distinct outcomes; a successful JSON empty result remains an empty read while blank/null provider output or any unrecognized result record is unavailable | recipient hash/domain only; case-preserving opaque message id hash; case-preserving requested returned trace-header value hash; allowlisted action label or `unknown`; validated in-window timestamp; counts only for distinct/duplicate keys; raw payload omitted | invalid/reversed window, invalid action, unsafe query token, invalid domain, blank/null provider output, malformed, unrecognized, malformed/null/conflicting identity or timestamp alias, invalid/out-of-window timestamp, filter-mismatching result record, unknown-action partial evidence, capped result limit, missing/malformed/conflicting authoritative source domain, no summarized events matching the requested source domain, duplicate recipient/message event summary | README, monitoring runbook, live-proof matrix | implemented |
| Exact message engagement | `oci_email_message_engagement` | read | exact non-blank Message-ID using the conservative identifier grammar with one optional enclosing angle-bracket pair; bounded UTC window; source domain required for proof; optional limit and compartment override | OCI Logging Search Email Delivery logs through the existing event parser | per-signal open/click/list-unsubscribe states and nullable counts; an unscoped request returns `unavailable` without a provider query; lane scope binds to the authoritative record `source` Email Domain rather than sender/envelope addresses; string UTC aliases and OCI numeric outer `datetime` epoch milliseconds must agree; complete uncapped exact evidence yields `proven_active` when count is positive and `not_observed` only for a complete zero; overall `proven_active` requires all three signals, while a complete subset is `not_observed`; the composed tool independently rebinds the backend report to the requested Message-ID hash, UTC window, source scope, limit, uncapped evidence, complete redacted event set and recomputed counts; overall status is `unavailable` for null/provider failure, capped or partial reads, contradictory report shape, unknown actions, source mismatch, malformed identity/timestamp, or other incomplete evidence; transport ingress is explicitly unavailable because the current parser does not inspect or attest SMTP/SubmitEmail ingress; duplicate summaries retain counts and never create a false absence; `send_authorized=false` | exact Message-ID hash, validated source domain, counts/status only, no raw event payloads | missing/blank or unsafe Message-ID, malformed/reversed UTC, absent/invalid source scope, null provider output, capped evidence, contradictory backend report, unknown action, source mismatch, numeric/string timestamp conflict, no events, subset signals, duplicate events, unavailable ingress | README, monitoring runbook, live-proof matrix | implemented |
| Logging configuration visibility | `oci_email_logging_status` | read | optional compartment override, optional Email Domain name, optional Email Domain/resource OCID, limit | OCI Email Domain list, OCI Logging log groups, and service-log list commands | active Email Delivery service logs are visible to the selected profile; optional requested `resource_domain` resolves to a redacted resource id; requested domain/id has an active matching visible service log with both matching and active-matching counts reported; when both domain and id are supplied they must resolve to the same Email Domain | log group ids, log ids, display names, and source resources are redacted or hashed; no raw log config JSON | no log groups, no Email Delivery service logs, no active service logs, requested domain not visible, requested resource not visible, requested domain/id conflict, requested resource visible but inactive, capped Email Domain/log group/service-log lists | README, monitoring runbook, live-proof matrix | implemented |
| Custom return-path inventory | `oci_email_return_paths` | read | optional compartment override, optional parent Email Domain OCID, limit | OCI Email Delivery custom return-path list | reports bounded custom return-path domains and lifecycle state, counts ACTIVE branded paths, and classifies the applicable routing as active branded custom return path or default OCI bounce handling; current OCI `data.items` and legacy `data` envelopes are accepted | OCIDs are redacted; CNAME values are hashed with presence only; lifecycle details and raw provider payload are omitted | invalid parent identifier, capped inventory, empty inventory/default handling, missing or non-string provider fields, raw OCID/DNS-target leakage | README, monitoring runbook | implemented |
| Logging enablement plan | `oci_email_logging_enablement_plan` | read / no-mutation plan | optional compartment override, optional Email Domain name, optional Email Domain/resource OCID, limit | Composed `oci_email_logging_status` plus static OCI Email Delivery logging guidance | operator-facing checklist for required service-log categories, permissions, approval boundary, and post-enable proof gates; a resolvable `resource_domain` can satisfy the resource-scoped no-apply proof without exposing raw OCIDs; unresolved target scope blocks without marking provider mutation required | raw OCIDs, log names, provider JSON, and recipient data are not returned; `send_authorized=false`; `provider_mutation_authorized=false` | blocked status requires explicit approval, missing active logs, missing or unresolved resource scope, partial/capped status evidence, status read failure | README, monitoring runbook, live-proof matrix | implemented |
| Seed/proof traceability | `oci_email_trace_message` | read | UTC window plus message id or correlation header; optional source domain | OCI Logging Search Email Delivery logs | related events grouped by case-preserving opaque message/correlation key and optional lane scope; returned records must read back the requested filter identity | message/header values hashed without case folding; raw payload omitted | missing trace criteria, unsafe header name/value, invalid source domain, case-distinct or other returned filter mismatch | README, live-proof matrix | implemented |
| Suppression reconciliation input | `oci_email_suppressions` | sensitive-read summary | optional window, limit | OCI Email Delivery suppression list with all pages fetched and a bounded returned sample | complete `total_matched`, bounded redacted sample rows, aggregate reason totals, capped recipient-domain totals with omitted-bucket count, count confidence, and timestamp bounds for clean-audience reconciliation | recipient domain plus hash only; reason/domain totals only; high-cardinality domain buckets capped | empty stdout, unsafe limit, sample cap metadata without total overclaim | README, live-proof matrix | implemented |
| Suppression active/delta receipt | `oci_email_suppression_delta` | sensitive-read summary | UTC window, optional limit | Composed full active and bounded-window OCI suppression reads | one receipt classifying whether the window has new active suppressions, how many active suppressions are outside the window, and whether full/window counts are complete, lower-bound, or unavailable | nested suppression summaries remain redacted; recipient domain plus hash only; `send_authorized=false` | full active read blocked, bounded read blocked, invalid/reversed UTC window, empty stdout, lower-bound/no-sample count state, new hard-bounce suppression, new complaint suppression, new other suppression reason | README, monitoring runbook, live-proof matrix | implemented |
| Send-window watch receipt | `oci_email_watch_window` | read | UTC window, lane source/resource domain or resource id, optional trace criteria, limit | Composed read-only calls to status, logging-status, Monitoring, Logging Search, and suppression list | one receipt containing component statuses, logging-configuration proof, stop-gate findings, trace status when requested, and suppression-window evidence | nested reports remain redacted; raw payload omitted; `send_authorized=false` | component command failure, missing logging/metric/log proof, capped result limit, invalid trace criteria, unscoped lane filters | README, monitoring runbook, live-proof matrix | implemented |
| Send-window readiness receipt | `oci_email_send_readiness` | read/sensitive-read summary | UTC window, lane domain/resource id, sender domain, campaign id, batch id, expected ledger rows, optional trace criteria, limit | Composed watch-window receipt plus configured local JSONL send ledger | one receipt proving monitoring evidence and local pre-submission ledger rows in the same window before interpreting a seed/cohort send; every matched row must carry service-specific OCI Email Delivery provider authority | nested reports remain redacted; campaign, batch, recipient, message, correlation, template, subject, and provider values are hashed; raw payload omitted; `send_authorized=false` | missing expected row count, row-count mismatch, missing ledger path, missing trace/recipient keys, missing/non-OCI/mixed-provider authority, capped/invalid ledger rows, component command failure | README, monitoring runbook, live-proof matrix | implemented |
| Exact traceability audit | `oci_email_traceability_audit` | read/sensitive-read summary | UTC window, lane domain/resource id, sender domain, optional campaign id, optional batch id, optional expected ledger rows, required message id or correlation header for exact proof, limit | Composed watch-window receipt plus configured local JSONL send ledger | v2 audit with `schema=oci-email-delivery.traceability-audit.v2`; separate complete/partial/unavailable combined-log and ledger states, plus not-requested trace state, before any exact-overlap evaluation; requested trace key narrows the ledger read before capping, while exact proof requires exactly one matching valid ledger row with unambiguous OCI Email Delivery provider authority and binds its case-preserving trace key and recipient to every returned provider trace event; multiple related lifecycle events remain admissible only when they share one complete message-id and recipient identity; on header traces, any present ledger message-id hash must also equal that provider event identity | nested reports remain redacted; opaque message/header trace values are hashed without case folding; campaign, batch, recipient, template, subject, and provider values retain their field-appropriate hash contracts; raw payload omitted; `send_authorized=false`; combined-log and trace summary scalars are nullable when incomplete or unavailable; ledger scalars are nullable when unavailable; `provider_evidence_available` means observed evidence only and never acceptance, relay, or exact proof | missing or partial log/trace/ledger reads, successful empty reads, malformed event records, case-distinct or other returned message/header identity mismatch, missing or heterogeneous provider trace identity, missing/non-OCI/contradictory ledger provider identity, header-trace ledger/provider message conflict, missing provider datapoints, missing trace criteria, zero or multiple ledger rows, expected row-count mismatch or zero, trace-key/recipient/same-event overlap failure, capped/invalid ledger rows | README, monitoring runbook, live-proof matrix | implemented with fixture-backed v2 contract coverage |
| Durable monitoring snapshot | `oci_email_monitoring_snapshot_artifact` | private artifact / no-mutation proof | UTC window, lane domain/resource id, optional trace criteria, optional `receipt_kind=send_readiness` with campaign id, batch id, expected ledger rows, optional `receipt_kind=traceability_audit` with trace criteria, optional safe artifact prefix; configured `OCI_MCP_SNAPSHOT_ROOT` | Composed watch-window, send-readiness, or traceability-audit receipt plus local private artifact write | generated direct-child JSON artifact containing one redacted receipt, with returned filename, byte count, SHA-256, root hash, receipt status, and decision | no raw root path, raw recipient, message id, header name/value, campaign id, batch id, OCID-shaped strings, or provider payload returned or persisted | missing snapshot root, relative root, symlink root, non-private Unix root permissions, unsafe prefix, unsupported receipt kind, missing send-readiness fields, artifact collision, component proof failures, redacted identifier that still matches provider-native OCID syntax | README, monitoring runbook, live-proof matrix | implemented |

Deferred rows:

| workflow | deferred tool/action | reason |
| --- | --- | --- |
| Send proof | `submit-email` or SMTP send | No-send monitoring readiness is the first boundary; sends require separate approval and a write-side ledger creation contract. |
| Log enablement | Email Domain log apply | mutates OCI configuration and requires preview/apply proof; `oci_email_logging_status` and `oci_email_logging_enablement_plan` are read-only visibility and planning proof only. |
| Suppression mutation | add/remove suppression | can alter audience eligibility and needs explicit approval plus reconciliation proof. |
| Connector Hub export | service connector apply | infrastructure mutation; polling SearchLogs is the first proof path. |
| Downstream campaign import/export/apply | contact import/export/apply | belongs outside this read-only OCI adapter. |
