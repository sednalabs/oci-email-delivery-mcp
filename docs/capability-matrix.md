# OCI Email Delivery MCP Capability Matrix

| workflow | tool | class | inputs | data source | proof | redaction | negative tests | docs | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| First-run readiness | `oci_email_status` | read | optional compartment override | OCI CLI profile, Email Delivery sender/domain/suppression list commands | profile can call read-only OCI APIs; sender/domain counts; suppression query outcome | profile name only, OCID hash, no raw sender local parts in public docs | missing compartment, missing profile, empty suppression stdout | README, live-proof matrix | implemented |
| Pilot aggregate monitoring | `oci_email_metrics` | read | `start_time`, `end_time`, optional `interval`, optional sender/domain filters | OCI Monitoring `oci_emaildelivery` namespace | fixed metric definitions discovered and summarized for the window | no raw OCIDs; query filters summarized | unavailable metric, empty stdout, invalid interval, capped missing evidence | README, live-proof matrix | implemented |
| Local send-ledger window | `oci_email_ledger_window` | sensitive-read summary | `start_time`, `end_time`, optional sender domain, campaign id, batch id, limit; configured `OCI_MCP_LEDGER_PATH` | private local JSONL send ledger | bounded redacted row summaries plus counts needed for later event correlation | provider/recipient/message/correlation/campaign/batch/template/subject values are hashed; recipient and sender domains only | missing ledger path, invalid JSON rows, capped rows, missing message/correlation key, missing recipient key | README, monitoring runbook, live-proof matrix | implemented |
| Event ingestion visibility | `oci_email_events` | read | UTC window, optional whitelisted action/message/header/domain filters, limit | OCI Logging Search Email Delivery logs | bounded event summaries from OutboundAccepted/OutboundRelayed logs | recipient hash/domain only; message id hash; raw payload omitted | invalid action, unsafe query token, capped result limit | README, live-proof matrix | implemented |
| Logging configuration visibility | `oci_email_logging_status` | read | optional compartment override, optional Email Domain/resource OCID, limit | OCI Logging log groups and service-log list commands | active Email Delivery service logs are visible to the selected profile; optional requested resource id has a matching visible service log | log group ids, log ids, display names, and source resources are redacted or hashed; no raw log config JSON | no log groups, no Email Delivery service logs, no active service logs, requested resource not visible, capped log group or service-log lists | README, monitoring runbook, live-proof matrix | implemented |
| Seed/proof traceability | `oci_email_trace_message` | read | UTC window plus message id or correlation header; optional source domain | OCI Logging Search Email Delivery logs | related events grouped by redacted message/correlation key and optional lane scope | message/header values hashed; raw payload omitted | missing trace criteria, unsafe header name/value, invalid source domain | README, live-proof matrix | implemented |
| Suppression reconciliation input | `oci_email_suppressions` | sensitive-read summary | optional window, limit | OCI Email Delivery suppression list | count/sample of suppression rows plus aggregate reason/domain totals for clean-audience reconciliation | recipient domain plus hash only; reason/domain totals only | empty stdout, capped result limit, unsafe limit | README, live-proof matrix | implemented |
| Send-window watch receipt | `oci_email_watch_window` | read | UTC window, lane source/resource domain or resource id, optional trace criteria, limit | Composed read-only calls to status, Monitoring, Logging Search, and suppression list | one receipt containing component statuses, stop-gate findings, trace status when requested, and suppression-window evidence | nested reports remain redacted; raw payload omitted; `send_authorized=false` | component command failure, missing metric/log proof, capped result limit, invalid trace criteria, unscoped lane filters | README, monitoring runbook, live-proof matrix | implemented |
| Send-window readiness receipt | `oci_email_send_readiness` | read/sensitive-read summary | UTC window, lane domain/resource id, sender domain, campaign id, batch id, expected ledger rows, optional trace criteria, limit | Composed watch-window receipt plus configured local JSONL send ledger | one receipt proving monitoring evidence and local pre-submission ledger rows in the same window before interpreting a seed/cohort send | nested reports remain redacted; campaign, batch, recipient, message, correlation, template, and subject values are hashed; raw payload omitted; `send_authorized=false` | missing expected row count, row-count mismatch, missing ledger path, missing trace/recipient keys, capped/invalid ledger rows, component command failure | README, monitoring runbook, live-proof matrix | implemented |
| Exact traceability audit | `oci_email_traceability_audit` | read/sensitive-read summary | UTC window, lane domain/resource id, sender domain, optional campaign id, optional batch id, optional expected ledger rows, required message id or correlation header for exact proof, limit | Composed watch-window receipt plus configured local JSONL send ledger | one audit that states whether OCI log events and one local ledger row overlap by trace key and recipient hash, or whether the evidence is aggregate-only | nested reports remain redacted; message/header values, campaign, batch, recipient, template, subject, and provider values are hashed; raw payload omitted; `send_authorized=false`; `aggregate_only=true` when exact proof is missing | missing trace criteria, no log events, no trace events, no ledger rows, expected row-count mismatch or zero, missing ledger path, no trace-key overlap, no recipient-hash overlap, split-row overlap, capped/invalid ledger rows | README, monitoring runbook, live-proof matrix | implemented |
| Durable monitoring snapshot | `oci_email_monitoring_snapshot_artifact` | private artifact / no-mutation proof | UTC window, lane domain/resource id, optional trace criteria, optional `receipt_kind=send_readiness` with campaign id, batch id, expected ledger rows, optional `receipt_kind=traceability_audit` with trace criteria, optional safe artifact prefix; configured `OCI_MCP_SNAPSHOT_ROOT` | Composed watch-window, send-readiness, or traceability-audit receipt plus local private artifact write | generated direct-child JSON artifact containing one redacted receipt, with returned filename, byte count, SHA-256, root hash, receipt status, and decision | no raw root path, raw recipient, message id, header name/value, campaign id, batch id, OCID, or provider payload returned or persisted | missing snapshot root, relative root, symlink root, non-private Unix root permissions, unsafe prefix, unsupported receipt kind, missing send-readiness fields, artifact collision, component proof failures | README, monitoring runbook, live-proof matrix | implemented |

Deferred rows:

| workflow | deferred tool/action | reason |
| --- | --- | --- |
| Send proof | `submit-email` or SMTP send | No-send monitoring readiness is the first boundary; sends require separate approval and a write-side ledger creation contract. |
| Log enablement | Email Domain log apply | mutates OCI configuration and requires preview/apply proof; `oci_email_logging_status` is read-only visibility proof only. |
| Suppression mutation | add/remove suppression | can alter audience eligibility and needs explicit approval plus reconciliation proof. |
| Connector Hub export | service connector apply | infrastructure mutation; polling SearchLogs is the first proof path. |
| Downstream campaign import/export/apply | contact import/export/apply | belongs outside this read-only OCI adapter. |
